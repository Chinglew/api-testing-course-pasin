info:
  name: login fail
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "studend",
        "password": "hungry"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return success", function () {
          expect(res.getStatus()).to.equal(401);
        });

        test("should contain error text", function () {
          expect(res.getBody()).to.have.property("error")
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
